#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-commit:
  jobs:
    - run: bunx lint-staged

prepare-commit-msg:
  commands:
    commitizen:
      skip: merge
      interactive: true
      run: bunx cz --hook
      env:
        LEFTHOOK: 0
