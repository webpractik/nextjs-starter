{
    "x-readme-ref-name": "UpdatePet",
    "type": "object",
    "additionalProperties": false,
    "minProperties": 1,
    "properties": {
        "name": { "type": "string", "minLength": 1, "maxLength": 120, "example": "Pixel II" },
        "status": {
            "type": "string",
            "description": "Current lifecycle status of a pet.",
            "enum": ["available", "pending", "sold"],
            "example": "available",
            "x-readme-ref-name": "pet-status"
        }
    }
}
