{
    "type": "object",
    "additionalProperties": false,
    "required": ["status", "code", "title"],
    "properties": {
        "status": { "type": "integer", "minimum": 400, "maximum": 599, "example": 404 },
        "code": { "type": "string", "minLength": 1, "example": "pet_not_found" },
        "title": { "type": "string", "minLength": 1, "example": "Pet not found" },
        "detail": { "type": "string", "example": "No pet exists for the supplied identifier." }
    },
    "x-readme-ref-name": "problem"
}
