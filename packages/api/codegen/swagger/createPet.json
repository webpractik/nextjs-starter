{
    "x-readme-ref-name": "CreatePet",
    "type": "object",
    "additionalProperties": false,
    "required": ["name"],
    "properties": {
        "name": { "type": "string", "minLength": 1, "maxLength": 120, "example": "Pixel" },
        "status": {
            "allOf": [
                {
                    "type": "string",
                    "description": "Current lifecycle status of a pet.",
                    "enum": ["available", "pending", "sold"],
                    "example": "available",
                    "x-readme-ref-name": "pet-status"
                }
            ],
            "default": "available"
        }
    }
}
