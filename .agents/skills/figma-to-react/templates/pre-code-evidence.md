# Сведения до начала реализации

Заполни документ до изменения кода, стилей, данных и ресурсов приложения. Замени все подсказки; пути указывай относительно проекта.

## Содержание

- [Совместимость](#совместимость-и-задача), [проект](#что-найдено-в-проекте), [Figma](#контекст-и-источники-figma), [ресурсы](#ресурсы-приложения)
- [Модель](#модель-экрана), [адаптивные выводы](#адаптивные-выводы), [компоненты](#решения-по-компонентам), [значения](#порядок-выбора-значений)
- [Захват](#контракт-захвата), [доступность](#план-доступности), [манифест](#план-манифеста-реализации), [журнал](#журнал-событий)

## Совместимость и задача

- Запись `G0`: `artifacts/visual/<task-id>/compatibility.md`
- Контракт задачи: `artifacts/visual/<task-id>/task-contract.json`
- База Git: `<repositoryRoot, profile, полный хеш из baseline.revision и точные preExistingChanges>`
- Точный URL Figma, `fileKey`, `fileVersion`, `nodeId`: `<значения из проверенного контракта>`
- Адрес проекта: `<точное значение $.target.origin>`
- Включено в работу: `<значения из $.scope.include>`
- Исключено из работы: `<значения из $.scope.exclude>`
- Разрешённые корни реализации: `<точный JSON $.target.allowedImplementationRoots>`
- Доступные инструменты и полномочия: `<значения из $.provisioning>`

## Что найдено в проекте

- Владелец маршрута и точки входа React: `<пути и вывод>`
- Стили, токены и контрольные ширины: `<пути и вывод>`
- Шрифты: `<семейства, стили, веса, файлы из проекта или пакет @fontsource>`
- Изученные компоненты и варианты: `<пути и вывод>`
- Команда снимка: `<точное значение $.provisioning.visualCaptureCommand>`
- Команда сравнения: `<точное значение $.provisioning.visualCompareCommand>`
- Команды `projectChecks` и `browserChecks`: `<пары id: command>`
- Воспроизводимые данные: `<путь $.data.fixture>`

## Контекст и источники Figma

- Реестр контекста: `artifacts/visual/<task-id>/design-context-manifest.json`
- Первый `get_design_context` корня: `<requestId, responseFormat text, путь .txt, SHA-256>`
- Точечные запросы: `<не требовались | потребовались из-за крупного или усечённого ответа>`
- Дополнительные операции: `<operation, responseFormat, путь, SHA-256 или не применялись>`
- Реестр источников: `artifacts/visual/<task-id>/source-manifest.json`
- Эталон и его происхождение: `<sourceId, прямой PNG или запись derivations>`

## Ресурсы приложения

| Узел Figma | Вид | Запись источника | Планируемый путь | Решение |
| --- | --- | --- | --- | --- |
| `<nodeId>` | `<svg / png / график>` | `<sourceId, checksum>` | `<путь в проекте>` | `<повторно использовать / создать на G4 / STOP>` |

## Модель экрана

### Модель компоновки

`<контейнеры, нормальный поток, Flex или Grid, порядок содержимого, размеры, вертикальный ритм>`

### Повторно используемые компоненты

`<компонент проекта, путь, совместимый API, семантика и визуальное соответствие>`

### Новые компоненты

`<новая граница, изученные кандидаты и точные причины отказа>`

### Токены

`<использованные токены и обоснование каждого нового значения>`

## Адаптивные выводы

Заполни по [правилам адаптивных выводов](../rules/mapping-and-implementation.md#адаптивные-выводы).

| ID области просмотра | Переход | Класс | Статус | Источник |
| --- | --- | --- | --- | --- |
| `<$.viewports[*].id>` | `<наблюдаемый переход>` | `<fluid / fixed / wrap / reordered / scrollable / точное описание>` | `<observed / inferred / unknown>` | `<Figma или путь проекта>` |

## Решения по компонентам

| Ответственность | Изученные кандидаты | Решение | Причина |
| --- | --- | --- | --- |
| `<часть интерфейса>` | `<пути компонентов>` | `<использовать / скомпоновать / создать / STOP>` | `<API, семантика и различия>` |

## Порядок выбора значений

Используй ступени из [правил реализации](../rules/mapping-and-implementation.md#порядок-выбора-значений).

| Свойство | Ступень | Источник | Обоснование |
| --- | --- | --- | --- |
| `<свойство>` | `<1 / 2 / 3 / 4 / 5 / 6>` | `<компонент, токен или шкала>` | `<почему это первая допустимая ступень>` |

## Контракт захвата

- Контракт захвата: `artifacts/visual/<task-id>/capture-contract.json`
- Полная матрица: `<для каждой required state есть случай с каждым viewport: да | STOP>`
- Значения `not-applicable`: `<state и непустое evidence>`
- Единый отпечаток среды: `<browser, os, container, headless, отсортированные fontHashes>`
- Готовность: `<readiness содержит только структурированные записи с допустимым kind; JavaScript из контракта отсутствует>`
- Фактические размер и DPR будут проверены: `<да | STOP>`
- Применимость дополнительных режимов: `<behavior-only: required или not-applicable и evidence; responsive-only: required или not-applicable и evidence>`
- Режим каждого случая: `<пары caseId: evidenceMode>`

## План доступности

- `behavior-only`: `<сценарии keyboard, focus, accessible-name, contrast и селекторы>`
- `responsive-only`: `<сценарии overflow, reading-order, reflow, zoom и селекторы>`

## План манифеста реализации

- Файлы, которые могут войти в `files`: `<пути к коду, стилям, ресурсам, данным и зависимостям>`
- Правило расчёта `rootHash`: [манифест реализации](../rules/mapping-and-implementation.md#манифест-реализации-и-implementationhash)
- Будущая неизменяемая копия: `определится после G4 по rootHash`

## Журнал событий

- Журнал: `artifacts/visual/<task-id>/evidence-log.jsonl`
- События `G0`, `G1`, `G2`, `G3`: `<последовательные записи с implementationHash: null>`
- Последний закрытый этап: `<G3 | более ранний этап и причина STOP>`
