// Automatically generated by declarative-routing, do NOT edit
import { z } from 'zod';
import { makeGetRoute, makeRoute } from './makeRoute';

const defaultInfo = {
    search: z.object({}),
};

import * as HomeRoute from '~/app/(home)/page.info';
import * as ApiHealthRoute from '~/app/api/health/route.info';

export const Home = makeRoute('/(home)', {
    ...defaultInfo,
    ...HomeRoute.Route,
});

export const getApiHealth = makeGetRoute(
    '/api/health',
    {
        ...defaultInfo,
        ...ApiHealthRoute.Route,
    },
    ApiHealthRoute.GET
);
